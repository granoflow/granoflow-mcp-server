agent:
  name: granoflow-first-run-import
  short_description: "Initialize Granoflow, install recommended AI capabilities, and optionally import agent data"
  default_prompt: "Use the Granoflow First-Run Import skill when the user asks to initialize Granoflow, optionally with data import, including localized equivalents. After connection, offer Pandoc+Mermaid CLI consent for token-free Markdown→HTML Plan acceptance previews (declining keeps Markdown-only). Then show only the names and plain-language functions of unavailable recommended external capability collections and recommend installing all of them. Ask one final time after the first refusal, then respect a second refusal. Do not run project-level Matt Pocock setup during global initialization. Continue to a confirmed import preview only when data import was requested."
