interface:
  display_name: Granoflow Integration Test Campaign
  short_description: Orchestrate minimal service_path IT (cross-module real I/O), AI auto-drive, triage/fix until green; plain-language closing summary. Not E2E UI/screenshots.
  default_prompt: Run an integration test campaign—orchestrate a minimal service_path suite, auto-drive until green, and finish with a beginner plain-language closing summary that points next to the E2E campaign.
